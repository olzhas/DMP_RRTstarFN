#
sudo: required
# Enable C++ support
language: cpp
# Compiler selection
compiler:
  - clang
  - gcc
before_install:
# Install necessary packages from apt-get
 - "./misc/travis_before_install.sh"
# Build steps
script:
  - mkdir build
  - cd build
  - cmake .. && make

